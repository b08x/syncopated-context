{
  "description": "Rubysmithing convention enforcement hooks",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash $CLAUDE_PLUGIN_ROOT/skills/plan/hooks/scripts/check-ruby-conventions.sh",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"prompt\": \"If you generated or modified any Ruby (.rb) files during this conversation, add a brief note at the end of your response suggesting the user run /rubysmithing:report for a SIFT QA assessment. Only add this note if Ruby files were actually written — omit it for explanations, research-only tasks, or conversations where no .rb files were produced.\"}'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
