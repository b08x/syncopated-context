interface:
  display_name: "Syncopated Context"
  short_description: "Repo-specific patterns and workflows for syncopated-context"
  default_prompt: "Use the syncopated-context repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true